
## 📝 Лабораторна робота №3: Маніпулювання даними (OLTP)

**Мета:** Відпрацювання операцій DML (INSERT, SELECT, UPDATE, DELETE) на створеній базі даних.

### Виконані сценарії:

1.  **Вставка даних (`INSERT`):**
    * Успішно додано нового юзера (*'jack_rassel','fladdd123@gmail.com','Ukraine'*).

2.  **Вибірка даних (`SELECT`):**
    * Виконано пошук усіх юзерів у системі які мають початок на літеру А.
    * Виконано пошук юзера який має айді 6".
    *  Виконано пошук юзера який має юзернейм "john_travel".

3.  **Оновлення даних (`UPDATE`):**
    * Змінено username та country студента. Використано обмеження `WHERE user_id = ...` для безпеки даних.

4.  **Видалення даних (`DELETE`):**
    * Видалили юзера який мав айді 1
    * Видалив всіх юзерів з країни Італія

### Результати виконання запитів:

**1. Етап INSERT (Додавання даних):**
Відображення нових користувачів у системі.
![image](/lab3/screenshots/Screenshot%202025-12-02%20at%2020.57.21.png)

**2. Етап SELECT :**
![image](/lab3/screenshots/Screenshot%202025-12-02%20at%2020.58.17.png)


**3. Етап UPDATE (Оновлення даних):**
Демонстрація зміни прізвища та пошти студента.
![image](/lab3/screenshots/Screenshot%202025-12-02%20at%2021.00.25.png)

**4. Етап DELETE (Видалення даних):**
Підтвердження видалення тестового користувача (результат порожній).
![image](/lab3/screenshots/Screenshot%202025-12-02%20at%2020.49.33.png)

Ми практикували різні SQL-запити (це називається OLTP), щоб подивитися, як воно поводиться.
В основному ми спробували чотири команди: `SELECT` для вибірки даних (причому не просто `*`, а конкретні стовпці та з фільтром `WHERE`),
 `INSERT` для додавання нових рядків,
  `UPDATE` для зміни старих (обов'язково з `SET` та `WHERE`) та `DELETE` для видалення.
Найголовніший момент,на якому наголошували, — це **ніколи не забувати `WHERE`** в `UPDATE` та `DELETE`. 
 Якщо забудеш, то знесеш або зміниш взагалі всі дані в таблиці, а це, зрозуміло, біда.
 Ну голове що дала мені ця лабораторна це те що я навчився користуватися фільтрами і апдетити дані в таблиці так щоб не заапдейтити всю таблицю.